# DFNS service account credentials
DFNS_API_URL=https://api.dfns.io
DFNS_ORG_ID=
DFNS_AUTH_TOKEN=
DFNS_CRED_ID=
DFNS_PRIVATE_KEY=

# DFNS wallets (EVM wallets on Sepolia, funded with test ETH)
BANK_WALLET_ID=         # minter — does the most transactions
SENDER_WALLET_ID=       # receives the mint, sends the transfer
RECEIVER_WALLET_ID=     # receives the transfer

# Optional: override the default public Sepolia RPC
# BLOCKCHAIN_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
